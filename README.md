# Welcome to your CDK TypeScript project

This is a Slackbot project for CDK development with TypeScript, connected to a source repository in GitHub.

The `cdk.json` file tells the CDK Toolkit how to execute your app.

Please modify the TODOs with necessary information.

## Useful commands

* `npm run build`   compile typescript to js
* `npm run watch`   watch for changes and compile
* `cdk bootstrap`   create the CDK Toolkit stack in order to deploy the rest of the CDK stacks
* `cdk deploy`      deploy this stack to your default AWS account/region
* `cdk diff`        compare deployed stack with current state
* `cdk synth`       emits the synthesized CloudFormation template
